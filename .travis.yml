language: python
# supported Python versions
python:
        - "2.6"
        - "2.7"
        - "3.2"
        - "3.3"
        - "3.4"
        - "3.5"
# command to install dependencies
install:
        - pip install pytest pytest-cov
        - pip install .

# command to run tests
script: py.test capidup
